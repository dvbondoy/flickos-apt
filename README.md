# FlickOS apt repository

This branch is published via GitHub Pages and serves the FlickOS apt
repository (packages only). It is populated by `repo/upload.sh` in the
main FlickOS repository — do not edit files here by hand.

ISO downloads are hosted separately; see the main FlickOS repository's
docs/05-apt-repository.md for details.
